const expect = require('chai').expect

describe('Greeting!.', () => {
  describe('Programming tips', () => {
    it('Arrays start at 1', () => {
      expect(false, 'I almost forgot those days with FoxPro.').to.equal(true)
    })
  })
})
