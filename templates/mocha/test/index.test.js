const expect = require('chai').expect

describe('Greeting!.', () => {
  describe('Programming tips', () => {
    it('Arrays start at 1', () => {
      expect(false, 'No way.').to.equal(true)
    })
  })
})
